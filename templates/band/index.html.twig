{% extends 'base.html.twig' %}

{% block title %}Band index{% endblock %}

{% block body %}

    <h1>Band index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Country</th>
                <th>City</th>
                <th>StartYear</th>
                <th>EndYear</th>
                <th>Founder</th>
                <th>NumberOfMembers</th>
                <th>MusicalType</th>
                <th>Presentation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for band in bands %}
            <tr>
                <td>{{ band.id }}</td>
                <td>{{ band.name }}</td>
                <td>{{ band.country }}</td>
                <td>{{ band.city }}</td>
                <td>{{ band.startYear }}</td>
                <td>{{ band.endYear }}</td>
                <td>{{ band.founder }}</td>
                <td>{{ band.numberOfMembers }}</td>
                <td>{{ band.musicalType }}</td>
                <td>{{ band.presentation }}</td>
                <td>
                    <button>
                        <a href="{{ path('band_edit', {'id': band.id}) }}">éditer</a>
                    </button>
                    {{ include('band/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div>
        <button>
            <a href="{{ path('import') }}">Importer</a>
        </button>

    </div>

{% endblock %}
